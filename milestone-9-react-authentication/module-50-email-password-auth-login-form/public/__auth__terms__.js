/**
 * Login === means u already create account before, now getting inside to use the account
 * 
 * register === signup === create account [Never an account had before ,means creating account for the first time]
 */

/**
 * 1. create firebase project 
 * 2. get firebase config
 * 3. Enable Auth Method (Build > Authentication > Sign In method)
 * 4. initialize auth method and explore it
 * 5. use createUserWithEmailAndPassword(auth, email, password)
 */